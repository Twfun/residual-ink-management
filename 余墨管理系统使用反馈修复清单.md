# 余墨管理系统 · 使用反馈修复清单

> 版本：v1.2　|　适用项目：`residual-ink-management`（React 19 + Ant Design 5 + NestJS + Prisma/MariaDB + Tauri 桌面端）
> 本文档记录**用户实际使用后反馈的问题**与对应修复方案，每条均带 `- [ ]` 勾选框，完成即勾选。本文档只做方案说明，确认后再实施。

---

## 〇、总体进度

> ①分页　`1/1`　②库存操作栏　`1/1`　③匹配列表操作栏　`1/1`　④最近记录 period　`1/1`　⑤统计页面　`2/2`　⑥统计二级明细　`1/1`　⑦侧栏固定　`1/1`　⑧删除区间图　`1/1`　⑨库存/出库日期筛选　`2/2`　⑩取消桌面配置页　`1/1`　⑪界面英文改中文　`1/1`

---

## 一、背景

系统在交付使用后，用户在实际操作中反馈了若干**功能未接通**与**布局/页面调整**的需求，集中在分页、操作栏、最近记录筛选，并提出了**新增按月统计页面**的需求。以下逐条说明现状、修复方案与验收标准。

| #   | 问题 / 需求                                             | 类型          |
| --- | ------------------------------------------------------- | ------------- |
| 1   | 分页 10/20/50/100 每页切换功能未接入                    | 缺陷修复      |
| 2   | 余墨库存操作栏未接入出库、删除                          | 缺陷修复      |
| 3   | 颜色匹配结果列表未接入操作栏（仅需出库）                | 缺陷修复      |
| 4   | 最近库存、最近出库的「本日/周/月/年」切换未响应         | 缺陷修复      |
| 5   | 新增统计页面：按年/月筛选，展示本月每天出入库数量并合计 | 新功能        |
| 6   | 统计页双击某天弹出二级明细，出入库两页可切换查看        | 新功能        |
| 7   | 页面下滑时左侧功能区需固定                              | 布局调整      |
| 8   | 智能工作台删除「区间出入库统计」图表                    | UI 精简       |
| 9   | 余墨库存、余墨出库的筛选区域添加日期筛选                | 缺陷修复      |
| 10  | 取消桌面配置页面                                        | 页面下线      |
| 11  | 全界面英文文案改为中文                                  | 国际化/中文化 |

---

## 二、问题 1：分页每页条数切换未接入

**功能位置**：`apps/web/src/LegacyApp.tsx` 的 `SimpleTable` 组件。

**现状（代码事实）**：`SimpleTable` 内 `Table` 的 `pagination` 仅写了 `{ pageSize: 10 }`，未开启 `showSizeChanger`，页面无法切换 10/20/50/100 每页；`InventoryPage` 的表格虽开了 `showSizeChanger: true`，但未与后端分页参数联动，大数据量时仅靠前端全量内存分页。

**修复方案**：

- [x] **① 统一分页控件**
  - 在 `SimpleTable` 的 `pagination` 增加 `showSizeChanger: true` 与 `pageSizeOptions: ['10', '20', '50', '100']`，默认 `pageSize` 保持 10。
  - `InventoryPage` 表格同步 `showSizeChanger: true + pageSizeOptions`，并引入 `current`/`pageSize` state，切换时重新拉取。
- [x] **② 后端分页支持（可选增强）——按文档约定跳过：仅做前端分页（数据量小），未改后端接口**
  - `inventory.ts` 的 `list`、`outbound.ts` 的 `list` 增加 `page` / `pageSize` 参数，返回 `{ rows, total }`；复用既有 `where` 构造，`skip/take` 分页。
  - 前端 `InventoryPage` / `OutboundPage` 把 `page/pageSize` 透传，`Table` 用受控 `pagination`（`current/total/pageSize/onChange`）。
  - 若仅做前端分页即可满足，可跳过此项，标记 ✅ 在验收说明。

**验收标准**：表格右上角出现 10/20/50/100 下拉并可切换；切换后每页条数生效；配合后端分页时翻页数据正确、`total` 与真实条数一致。

---

## 三、问题 2：余墨库存操作栏未接入出库、删除

**功能位置**：`apps/web/src/LegacyApp.tsx` 的 `InventoryPage`。

**现状（代码事实）**：`InventoryPage` 操作列仅有「编辑」按钮（受 `inventory.update` Gate），无「出库」「删除」；后端已具备 `DELETE /inventory/:id`（`InventoryService.remove`，含出库记录保护）与 `POST /outbound`（出库），但前端未接入。

**修复方案**：

- [x] **删除按钮**
  - 操作列新增「删除」按钮，按 `inventory.delete` 权限 Gate，无权限隐藏。
  - 点击用 `Modal.confirm` 二次确认（提示「删除后不可恢复」）；确认后调 `DELETE /inventory/:id`，成功刷新列表并提示。
  - 后端已拦截「已有出库记录的库存不能删除」，前端展示后端错误信息即可。
- [x] **出库按钮（复用快捷出库）**
  - 操作列新增「出库」按钮，按 `outbound.create` 权限 Gate。
  - 点击弹出快捷出库对话框（与问题 3 共用一个 `QuickOutboundModal` 组件），预填库位与当前重量，提交 `POST /outbound`（单号自动生成），成功后刷新列表。

**验收标准**：有权限账号可见「编辑 / 出库 / 删除」三按钮；无权限分别隐藏对应按钮；删除有二次确认，已有出库记录的库存删除时提示后端错误；出库成功后该库位重量/状态更新。

---

## 四、问题 3：颜色匹配结果列表未接入操作栏（出库）

**功能位置**：`apps/web/src/LegacyApp.tsx` 的 `Match` 页结果表。

**现状（代码事实）**：`Match` 结果表直接用 `SimpleTable` 渲染，未传 `action` 列，无任何操作按钮；`SimpleTable` 本身支持可选 `action` 列渲染（需启用）。

**修复方案**：

- [x] **匹配结果出库**
  - `Match` 组件接收 `rights` prop（由 `LegacyApp` 传入 `user.permissions`）。
  - `SimpleTable` 增加可选 `action` 列；结果表新增「出库」按钮，仅 `rights.includes('outbound.create')` 时显示，否则整列隐藏。
  - 点击打开 `QuickOutboundModal`（与问题 2 共用），预填 `storageLocation / rollerColorCode / colorFamily / 当前库存 weightKg`，填写本次重量提交 `POST /outbound`。
  - 成功后提示「出库成功」并重跑当前搜索（用表单当前 `l/a/b` 与筛选参数），使该库位重量/状态在结果中刷新。

**验收标准**：有 `outbound.create` 权限账号点「出库」→ 填重量 → 提交后该库位重量/状态更新、结果刷新、出库记录与日志生成；无权限账号看不到「出库」按钮；重量超库存被拦截；库位重量未知时提示「库位重量未知，请确认后手动填写」。

---

## 五、问题 4：最近库存、最近出库的「本日/周/月/年」切换未响应

**功能位置**：`apps/web/src/LegacyApp.tsx` 的 `Dashboard`、`apps/api/src/dashboard.ts` 的 `overview`。

**现状（代码事实）**：前端 `Dashboard` 用同一个 `periodSwitch`（`Segmented`，`value={period}`）同时作为「最近库存」与「最近出库」两张表的 `extra`；`period` 变化时重新拉 `/dashboard?period=`。但存在两点导致**用户感知为未响应**：① 两张表共用同一 `period` 状态与同一 Segmented，切换时无法区分是哪张表在筛；② `overview` 的 period 过滤只作用于 `recentWhere = { createdAt >= periodStart }`，而两张表展示的是 `createdAt`/`outboundDate` 字段，过滤口径与展示字段不一致，数据可能无明显变化。

**修复方案**：

- [x] **拆分独立筛选控件**
  - 「最近库存」「最近出库」各自使用独立的 `Segmented`（`inventoryPeriod` / `outboundPeriod`）与独立 state，互不影响。
- [x] **对齐过滤口径**
  - 后端 `overview` 增加可选的 `inventoryPeriod` / `outboundPeriod`（或复用 `period` 分别作用于两表），「最近库存」按 `createdAt >= start` 过滤，「最近出库」按 `outboundDate >= start`（若无则 `createdAt`）过滤，保证切换后数据可见变化。
  - 前端把两个 Segmented 的值分别传给 `/dashboard?inventoryPeriod=&outboundPeriod=`。

**验收标准**：最近库存与最近出库可独立切换本日/本周/本月/本年；切换后对应表数据按口径明显收窄；两表互不干扰；默认「全部」显示全量。

---

## 六、问题 5：新增统计页面（月度出入库日报 + 合计）

**需求**：新增一个统计页面，仅提供**年份 / 月份**筛选，展示所选月份**每一天**的入库数量与出库数量，并在末尾**合计本月**入库、出库总数。

**方案**：

- [x] **⑤-a 后端统计端点**
  - 新建 `apps/api/src/statistics.ts`（或并入 `dashboard.ts`）：
    - `GET /statistics/monthly?year=2026&month=6` → `{ year, month, days: [{ date: '2026-06-01', inbound: number, outbound: number }, ...], total: { inbound, outbound, days } }`。
    - 逻辑：`residualInk` 按 `inboundDate` 落在该月分组计数入库；`outboundRecord` 按 `outboundDate` 落在该月分组计数出库；用 1..月末天数补齐空日（inbound/outbound 为 0）；`total` 汇总当月。
    - `year`（默认当年）、`month`（默认当月）参数校验，权限复用 `dashboard.view`。
- [x] **⑤-b 前端统计页面**
  - `contracts.ts` 的 `PAGE_CONTRACT` 增加 `['statistics', '出入库统计', 'dashboard.view']`，侧栏菜单新增「出入库统计」入口。
  - 新建 `StatisticsPage` 组件并接入 `LegacyApp` 路由；页面顶部两个 `Select`（年份 `2020..当年`、月份 `1..12`，默认当年当月），下方 `Table` 列出该月每天：`日期 / 入库数量 / 出库数量 / 当日合计`，底部一行 `本月合计：入库 x 条 / 出库 y 条`。
  - 新增 `styles.css` 少量样式（`.stats-total-row` 等）。

**验收标准**：切换年份/月份后表格刷新为该月每日数据；空日显示 0；底部合计与当月各日之和一致；无数据月份正常显示 0 合计；菜单可见且受 `dashboard.view` 权限控制。

---

## 七、问题 6：统计页双击某天弹出该日出入库明细

**需求**：在统计页双击某一天的记录，弹出**二级明细列表**，展示这一天具体的出入库记录，通过**「入库 / 出库」两个页签切换**查看。

**方案**：

- [x] **⑥-a 后端日明细端点**
  - 新增 `GET /statistics/daily?date=2026-06-15&kind=inbound` / `&kind=outbound`：
    - `kind=inbound`：返回该日 `residualInk`（`inboundDate` 为该日）列表，字段含 `storageLocation / rollerColorCode / weightKg / colorFamily`。
    - `kind=outbound`：返回该日 `outboundRecord`（`outboundDate` 为该日）列表，字段含 `outboundNo / storageLocation / rollerColorCode / weightKg / colorFamily`。
    - 权限复用 `dashboard.view`。
- [x] **⑥-b 前端双击明细弹窗**
  - 统计页表格行绑定 `onRow` 双击（`onDoubleClick`）事件，双击某天打开 `DailyDetailModal`。
  - 弹窗标题：「2026-06-15 出入库明细」，内部用 `Tabs`，两个页签「入库」「出库」，各自独立 `Table` 展示 `kind` 对应明细；页签切换分别请求 `/statistics/daily`。
  - 明细为空时显示空态占位。

**验收标准**：双击某天弹出明细弹窗；「入库 / 出库」页签可切换查看对应记录；数据与当天统计数字一致；无记录日双击显示空态；关闭弹窗可再次双击查看其他日期。

---

## 八、问题 7：页面下滑时左侧功能区需固定

**功能位置**：`apps/web/src/LegacyApp.tsx` 的 `Layout.Sider`、`styles.css` 的 `.rim-shell` / `.enterprise-sider`。

**现状（代码事实）**：`Layout.Sider` 高度随内容增长，右侧内容下滑时左侧 `Menu` 随之滚动离屏，用户需滚回顶部才能切换功能页。

**修复方案**：

- [x] **侧栏固定 + 内容独立滚动**
  - `.rim-shell` 设为 `height: 100vh; overflow: hidden`，防止整页滚动。
  - `.enterprise-sider` 设为 `position: sticky; top: 0; height: 100vh; overflow-y: auto`，品牌区 `.rim-brand` 保持固定、仅菜单区在侧栏内部可滚动。
  - 右侧 `Layout.Content` 单独 `overflow-y: auto`，实现「内容滚动、侧栏吸附」效果。

**验收标准**：拖动右侧页面下滑时，左侧品牌区与菜单始终固定在视口内；菜单项过多时仅在侧栏内部滚动；折叠侧栏（72px）后行为一致。

---

## 九、问题 8：智能工作台删除「区间出入库统计」图表

**功能位置**：`apps/web/src/LegacyApp.tsx` 的 `Dashboard`、`apps/api/src/dashboard.ts` 的 `overview`。

**现状（代码事实）**：`Dashboard` 渲染了「区间出入库统计」图表（ECharts），用户反馈信息冗余、无必要。

**修复方案**：

- [x] **移除图表区块**
  - 删除 `Dashboard` 中该图表的 EChart 渲染块及其数据字段引用；后端 `overview` 中仅服务于该图表的聚合字段一并移除（保留 `recentWhere`、最近记录等其余数据）。
  - 不影响「最近库存」「最近出库」两张表与顶部统计卡片的展示。

**验收标准**：进入智能工作台后不再出现「区间出入库统计」图表；其余卡片与最近记录表正常渲染；后端无该图表相关报错。

---

## 十、问题 9：余墨库存、余墨出库筛选区添加日期筛选

**功能位置**：`apps/web/src/LegacyApp.tsx` 的 `InventoryPage`、`OutboundPage`。

**现状（代码事实）**：库存/出库页筛选区仅有关键词、状态等条件，无法按日期范围筛选，用户难以定位特定时间段的记录。

**修复方案**：

- [x] **库存页日期筛选**
  - `InventoryPage` 筛选区新增 `DatePicker.RangePicker`（`startDate` / `endDate`），按 `inboundDate` 过滤；`inventory.ts` 的 `list` 在 `where` 增加 `inboundDate: { gte, lte }`。
- [x] **出库页日期筛选**
  - `OutboundPage` 筛选区新增 `DatePicker.RangePicker`，按 `outboundDate` 过滤；`outbound.ts` 的 `list` 在 `where` 增加 `outboundDate: { gte, lte }`。
  - 日期为空时不过滤；与关键词、状态等既有条件可组合；清空日期恢复全量。

**验收标准**：库存/出库页可选起止日期筛选，结果分别按 `inboundDate`/`outboundDate` 正确收窄；清空恢复全量；与其它筛选条件组合生效。

---

## 十一、问题 10：取消桌面配置页面

**功能位置**：`apps/web/src/LegacyApp.tsx` 的 `ConfigPage`、`pages` 菜单数组。

**现状（代码事实）**：`ConfigPage` 提供本地 API/数据库端口、数据目录、备份目录等编辑，用户评估该页面在实际使用中无必要，需下线。

**修复方案**：

- [x] **移除页面与菜单入口**
  - 从 `pages` 数组删除 `['config', labels.config, 'config.manage', <SettingOutlined/>]`，侧栏不再显示「桌面配置」；删除 `LegacyApp` 中 `{active === 'config' && <ConfigPage .../>}` 分支。
  - `ConfigPage` 组件与 `config.manage` 权限点按需清理；若后端 `desktop-config` 端点仍被桌面端初始化使用，则仅下线前端入口、保留后端。
  - `PageKey` 联合类型移除 `'config'` 或保留但不再映射菜单与路由。

**验收标准**：侧栏不再出现「桌面配置」入口；登录后无法导航到该页；桌面端启动与「连接仪器」功能不受影响。

---

## 十二、问题 11：全界面英文文案改为中文

**功能位置**：`apps/web/src/main.tsx` 的 `ConfigProvider`、`contracts.ts`、`LegacyApp.tsx` 及各组件文案。

**现状（代码事实）**：部分 Ant Design 组件内置文案（分页「items/page」、空态「No data」、确认框按钮、日期选择器等）仍为英文，用户要求全界面中文化。

**修复方案**：

- [x] **配置 antd 中文 locale**
  - `ConfigProvider` 增加 `locale={zhCN}`（`import zhCN from 'antd/locale/zh_CN'`），覆盖分页、空态、日期选择器、弹窗默认按钮（确定/取消）等内置文案。
- [x] **自定义文案中文化**
  - 排查 `LegacyApp.tsx` / `contracts.ts` / 各组件的硬编码英文（分页「每页」、空态、按钮、校验提示、表格列名、表单 label），统一替换为中文；`PAGE_CONTRACT` 标签确认均为中文菜单名。
- [x] **日期与数字格式化**
  - 日期选择器与表格时间列统一 `dayjs` 中文格式（`YYYY-MM-DD HH:mm`），避免出现英文星期/月份。

**验收标准**：全界面无可视英文文案（品牌名、专业缩写如 X-Rite、L*/a*/b\*、CIEDE2000 等除外）；分页、空态、按钮、日期选择器均为中文；修改后无编译或运行回归。

---

## 十三、验收与验证

1. 每项修复后运行 `npm run typecheck`（双端 `tsc --noEmit`）与 `npm test`（api + web vitest），确保零回归。
2. 手动 `npm run dev` 逐项验证：分页切换、库存操作栏（出库/删除）、匹配结果出库、最近记录 period 独立筛选、统计页年月筛选与合计、统计页双击明细与页签切换、侧栏固定、区间图移除、库存/出库日期筛选、桌面配置页下线、界面中文化。
3. 桌面端 `npm run desktop:dev` 复核出库/匹配联动、桌面配置下线与「连接仪器」；可选 `scripts/playwright-ui-smoke.mjs` 冒烟回归。
4. 统计页新增端点建议补 `statistics.test.ts`（分桶边界、闰年 2 月、跨月、空月）。
5. 中文 locale 配置后建议视觉回归分页、空态、日期选择器、确认弹窗等关键文案。

---

## 十四、假设与说明

- **权限**：统计页与明细复用既有 `dashboard.view`，不新增权限点；「出库/删除」按钮沿用既有 `outbound.create / inventory.delete`。
- **分页**：优先统一前端分页控件；若库存/出库数据量不大，可只做前端分页，后端分页作为可选增强。
- **统计口径**：入库按 `inboundDate`、出库按 `outboundDate` 统计，与展示字段一致，避免问题 4 的过滤口径不一致重演。
- **快捷出库组件**：问题 2、3 共用同一 `QuickOutboundModal`，避免两份逻辑漂移。
- **桌面配置下线**：仅下线前端页面入口；`desktop-config` 后端端点是否保留取决于桌面端初始化是否仍引用，若保留则不动后端。
- **中文化范围**：仅改动显示文案与 locale 配置，不改业务逻辑与数据；品牌名与专业缩写保留英文。

---

_本文档为修复方案 + 进度清单。任务完成即在本文档勾选 `- [x]`，作为进度唯一记录。确认方案后再进入实施阶段。_
