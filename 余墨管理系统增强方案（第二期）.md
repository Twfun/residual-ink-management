# 余墨管理系统 · 功能增强方案（第二期：账号记忆 / 页面状态保持 / 图标与中文名）

> 版本：v1.0　|　适用项目：`residual-ink-management`（React 19 + Ant Design 5 + NestJS + Prisma/MariaDB + Tauri 2 桌面端）
> 本文档为**纯设计方案 + 实施清单**，每条任务均带 `- [ ]` 勾选框。**完成即在本文档勾选**，作为进度的唯一记录。确认方案后再进入实施阶段。
> 与《余墨管理系统增强方案.md》（第一期，已全部实施）互相独立，本文件仅覆盖第二期三项增强。

---

## 〇、Plan 使用说明（勾选操作）

- 每个阶段下都有「阶段进度」总表与「任务清单」两个视图，二者指向同一组任务，任选其一勾选即可（建议以「任务清单」为准）。
- **勾选规则**：任务完成并通过该任务「验收标准」后，把 `- [ ]` 改为 `- [x]`；同时更新顶部「总体进度」的 `已完成/总数` 计数。
- **回滚规则**：若某任务上线后需回退，改回 `- [ ]` 并在该任务下追加一行 `> 回滚：<原因/时间>`，不删除记录。
- **纪律**：一个任务只勾一次；不允许「半完成」勾选，未过的验收标准一律保持 `- [ ]`。

### 总体进度

> 阶段 1：`4/4`　阶段 2：`3/3`　阶段 3：`6/6`

---

## 一、背景与目标

「余墨管理系统」第一期增强（可视化、导出、测量、匹配深化、UI 统一）已全部实施上线。经日常使用反馈，仍有三个体验问题需要解决：

| # | 问题 | 现状根因 | 优先级 |
| - | ---- | -------- | ------ |
| ① | 登录账号无法记忆，每次打开都要重新输入账号 | `Login` 组件账号为普通 `Input`，无任何持久化 | 高 |
| ② | 颜色匹配后切到其他页面再返回，匹配结果丢失；其他页面筛选后切回同样丢失 | 页面用 `{active===x && <Page/>}` 条件渲染，切换时组件卸载、`useState` 全部清空 | **优先** |
| ③ | 软件默认英文名 `ResidualInkManagement`，安装后桌面/任务栏/安装包均为英文；图标沿用默认 | `tauri.conf.json` 的 `productName` 为英文，图标为默认 `icon.ico` | 正常 |

**目标**：
1. **记住账号（不记住密码）**：登录页可从历史账号下拉选择，每次打开软件默认填入上次登录账号。
2. **页面状态保持**：切换页面后返回，颜色匹配结果、各页筛选条件、表单输入、弹窗状态全部保留（会话内 keep-alive）。
3. **中文品牌与图标**：安装包、已安装名称、开始菜单/任务栏/窗口标题统一为中文「余墨管理系统」，并更换品牌图标与登录页 logo。

**约束**：本方案**不含任何代码改动**，仅设计；不新增后端接口、不新增数据库字段、不新增权限点；沿用品牌主色 `#16796d` 与深色登录色 `#062f2c`；页面状态保持仅限**单次登录会话内**，退出登录即清空（符合预期）。

---

## 二、当前状态分析

### 2.1 登录流程现状（对应问题①）

- `apps/web/src/LegacyApp.tsx` 的 `Login` 组件（约 262–316 行）账号字段为 `Form.Item name="username"` + `<Input>`，`Form` 的 `initialValues={{ username: 'admin' }}` 每次固定默认 `admin`。
- 顶层 `LegacyApp` 的 token 仅在内存 `useState` 中，且挂载时执行 `localStorage.removeItem('rim-token')`，不持久化 token，也不持久化账号。
- 桌面端 Tauri 使用 WebView2，`window.localStorage` 持久化到应用数据目录，**可跨软件重启保留**——这是「记住账号」的天然载体。

### 2.2 页面状态丢失的根因（对应问题②）

- `LegacyApp` 内容区（238–250 行）为条件渲染：
  ```tsx
  {active === 'match' && <Match token={token} rights={user.permissions} />}
  {active === 'inventory' && <InventoryPage token={token} rights={user.permissions} />}
  ...
  ```
- 每次点击侧栏菜单 `setActive(key)` 后，上一个页面组件被**卸载**（unmount），其全部 `useState` 随之销毁：
  - `Match`：`result`（匹配结果）、`history`（测量历史）、表单 `l/a/b/公式/色差/top-N/色系` 值、`lastSearch`。
  - `InventoryPage`：`filters`、`rows`（当前列表）、`filterForm` 值。
  - `OutboundPage`：`keyword`、`range`、`lines`（待出库明细）、`outboundNo`、`outboundDate`。
  - `Dashboard`：图表维度选择、两张最近记录表。
- 返回该页面时重新挂载，`useEffect(..., [])` 重新拉取默认数据，`lastSearch` 为 `null`，匹配结果自然消失——与用户反馈完全一致。

### 2.3 桌面端名称与图标现状（对应问题③）

- `apps/desktop/src-tauri/tauri.conf.json`：
  - `productName: "ResidualInkManagement"`（决定 exe 名、安装包名、安装目录、开始菜单名）。
  - `identifier: "com.residualink.management.desktop"`（须保持 ASCII，不可中文化）。
  - 窗口标题 `app.windows[].title` 已是「余墨管理系统」。
  - `bundle.icon: ["icons/icon.ico"]`，`bundle.windows.nsis.languages: ["SimpChinese"]`。
- 现有安装包 `安装包/ResidualInkManagement_1.0.0_x64-setup.exe`。
- `apps/desktop/src-tauri/installer-hooks.nsh` 中按进程名 `ResidualInkManagement.exe` 结束残留进程（安装/卸载前）。
- `scripts/prepare-desktop.mjs` 的 `iconSource` 指向仓库外 `../apps/desktop/src-tauri/icons/icon.ico`，构建时同步到仓库内 `apps/desktop/src-tauri/icons/icon.ico`——**更换图标需两处同步**。
- 前端品牌图：`apps/web/src/assets/login-logo.png`（登录页标题 logo）、`login-scene.jpg`（登录背景）；侧栏品牌图标当前为 `DatabaseOutlined`（antd 图标）。

---

## 三、分阶段增强方案

> 路径均以 `d:\Projects\Product Color Sample Management System V4.0\residual-ink-management\` 为根。每阶段独立可交付、可回归、可回滚。实施顺序：阶段 2（优先）→ 阶段 1 → 阶段 3。

---

### 阶段 1：登录账号记忆（问题①）

**目标**：登录页支持从历史账号下拉选择；每次打开软件默认填入上次登录账号；**不记住密码**（密码框每次打开为空）。

**阶段进度**：`4/4`

#### 任务清单

- [x] **1.1 新建 `apps/web/src/session.ts`（记住账号工具，纯函数）**

  - **改动内容**：新增账号记忆的读写工具，供 `Login` 组件与测试复用。
  - **常量与签名**：
    - `export const REMEMBERED_USERS_KEY = 'rim-remembered-usernames';`
    - `export const MAX_REMEMBERED_USERS = 10;`
    - `export function loadRememberedUsernames(): string[]`：读 `localStorage[REMEMBERED_USERS_KEY]`，容错解析 JSON（非法/缺失返回 `[]`），逐项 `.trim()` 去空，去重保序。
    - `export function saveRememberedUsername(username: string): string[]`：`username.trim()` 后从旧列表移除旧条目，插入表头，截断到 `MAX_REMEMBERED_USERS`，写回 `localStorage`，返回新列表。
    - `export function clearRememberedUsernames(): void`：`localStorage.removeItem(REMEMBERED_USERS_KEY)`。
  - **安全要点**：**仅存账号字符串，绝不写密码**；密码框不持久化、不提供「记住密码」选项。
  - **新增依赖**：无。
  - **验收标准**：`session.test.ts` 覆盖——空存储返回 `[]`；写入去重且最新在前；超过 `MAX_REMEMBERED_USERS` 截断；非法 JSON 容错；`clear` 后归零。

- [x] **1.2 `apps/web/src/LegacyApp.tsx` `Login` 组件改造**

  - **改动内容**：账号字段由 `Input` 改为 `AutoComplete`（该组件已在文件顶部导入），并接入 `session.ts`。
  - **状态**：组件内新增 `const [usernames, setUsernames] = useState<string[]>(() => loadRememberedUsernames());`
  - **初始化**：`Form` 的 `initialValues={{ username: usernames[0] ?? 'admin' }}`——**默认填入上次登录账号**（列表首位），无历史时回退 `admin`。
  - **字段 UI**：`Form.Item name="username"` 内改为：
    - `<AutoComplete options={usernames.map(u => ({ value: u }))} filterOption>`，`filterOption` 按包含匹配过滤；`onChange` 时若值不在 `usernames` 中则不进列表（仅作为新输入）。
  - **登录成功写入**：`submit` 的 `onLogin` 成功回调前调用 `setUsernames(saveRememberedUsername(values.username))`。
  - **账号清除入口**：登录卡片底部加次要链接「清除历史账号」，仅当 `usernames.length > 0` 时显示；点击 `setUsernames(clearRememberedUsernames())` 并用 `message.success('已清除历史账号。')` 提示，账号框回退 `admin`。
  - **密码字段**：保持 `<Input.Password>` 不变，登录成功/失败均不写入任何存储。
  - **新增依赖**：无。
  - **验收标准**：首次打开默认 `admin`；登录成功后账号进入历史；再次打开软件（重启桌面端）账号框默认填入上次账号；下拉可历史选择；清除后历史为空且回退 `admin`；密码框始终为空、无任何持久化。

- [x] **1.3 `apps/web/src/styles.css` 登录区样式**

  - **改动内容**：新增 `.login-remember-bar`（账号下拉下方、密码上方或登录框底部的操作行，`display:flex; justify-content:space-between; align-items:center; font-size:13px`）；`.login-clear-link`（次要链接，`color` 用中性灰，`cursor:pointer`）。
  - **新增依赖**：无。
  - **验收标准**：登录卡片在 900px/560px 下无挤压；清除入口与下拉符合深色登录面板配色。

- [x] **1.4 新建 `apps/web/src/session.test.ts`**

  - **改动内容**：vitest 中对 `loadRememberedUsernames / saveRememberedUsername / clearRememberedUsernames` 做单测；用 `vi.stubGlobal('localStorage', ...)` 或直接操作 `window.localStorage` 隔离。
  - **验收标准**：`npm --workspace apps/web run test` 通过，覆盖 1.1 所列全部断言。

**阶段 1 验证**：`npm --workspace apps/web run test` 通过；`npm run typecheck` 无错；桌面端重启后账号框默认上次账号、下拉可选历史、清除可用、密码框始终为空。

---

### 阶段 2：页面切换状态保持（keep-alive）（问题② · 优先）

**目标**：单次登录会话内，切换页面后返回时，颜色匹配结果、各页筛选条件、表单输入、弹窗状态全部保留；同时避免启动时 8 页全量请求。

**方案**：将内容区从「条件渲染卸载」改为「**懒挂载 + 常驻隐藏**」（标准 keep-alive）：页面首次被激活才挂载，之后切换仅用 `display:none` 隐藏，组件不卸载、`useState` 不丢失。

**原则**：不改动各页面内部逻辑；仅改 `LegacyApp` 内容区渲染方式与少量样式。

**阶段进度**：`3/3`

#### 任务清单

- [x] **2.1 `apps/web/src/LegacyApp.tsx` 内容区 keep-alive 改造**

  - **改动内容**：把 238–250 行的条件渲染改为 keep-alive 常驻渲染。
  - **新增状态（`LegacyApp` 内）**：`const [keepAliveKeys, setKeepAliveKeys] = useState<PageKey[]>(['dashboard']);`（默认只挂载工作台）。
  - **激活加入**：侧栏菜单 `onClick` 里 `setActive(key)` 后追加 `setKeepAliveKeys(prev => prev.includes(key) ? prev : [...prev, key]);`（懒加载：首访才挂载）。
  - **渲染结构**：定义 `renderPage(key: PageKey)` 返回对应页面元素（映射关系与原条件渲染一致，`Dashboard` 保留其 `ConfigProvider fontSize 14` 包裹），内容区改为：
    ```tsx
    <div className="enterprise-page-shell">
      {keepAliveKeys.map((key) => (
        <div key={key} className="rim-keepalive-page" style={{ display: key === active ? 'block' : 'none' }}>
          {renderPage(key)}
        </div>
      ))}
    </div>
    ```
  - **行为说明**：
    - 首次激活某页才挂载并触发其 `useEffect(..., [])` 数据请求（避免启动 8 页全量请求）。
    - 已激活页面切换时仅 `display:none`，组件常驻，`result / filters / form / history / modal` 等 `useState` 全部保留。
    - 退出登录时 `token/user` 置空，整个主布局（含全部 keep-alive 页面）卸载，状态清空——符合预期。
  - **新增依赖**：无。
  - **验收标准**：`Match` 匹配后切走再切回，结果与表单保留；`InventoryPage` 筛选后切回，列表与筛选保留；`OutboundPage` 待出库明细/日期保留；`Dashboard` 维度选择保留；首次仅工作台请求数据，其余页首访才请求。

- [x] **2.2 `apps/web/src/styles.css` keep-alive 样式**

  - **改动内容**：新增 `.rim-keepalive-page { height: 100%; }`（`.enterprise-page-shell` 为滚动容器的场景下确保隐藏页不撑开布局）；隐藏页 `display:none` 不占用空间。
  - **滚动位置**：说明——因隐藏页常驻 DOM，切回时**滚动位置自动保留**，无需额外处理。
  - **新增依赖**：无。
  - **验收标准**：900px/560px 下活动页正常滚动、隐藏页不出现滚动条或空白；echarts 图切回后自适应尺寸正常（`ResizeObserver` 触发 `resize`）。

- [x] **2.3 页面切换状态保持验证**

  - **改动内容**：无代码改动，仅验收；逐页走查并记录。
  - **验收标准**（逐项勾选）：
    - [ ] 颜色匹配：输入 L/a/b → 匹配 → 切到「余墨库存」→ 切回「颜色匹配」，结果表、表单值、测量历史完整保留。
    - [ ] 余墨库存：设置筛选（关键词/状态/日期/targetLab）→ 切走再切回，列表与筛选保留。
    - [ ] 余墨出库：添加待出库明细、改日期 → 切走再切回，明细与日期保留。
    - [ ] 智能工作台：切换图表维度（日/周/月/年）→ 切走再切回，维度保留。
    - [ ] 退出登录后重新登录，全部页面恢复初始状态（无残留）。

**阶段 2 验证**：`npm run typecheck` 无错；`npm run dev` 手动执行 2.3 全部走查并勾选；无控制台报错。

---

### 阶段 3：软件图标更换与中文安装名（问题③）

**目标**：安装包、已安装名称、开始菜单/任务栏/窗口标题统一为中文「余墨管理系统」，并更换品牌图标与登录页 logo。

**阶段进度**：`6/6`

#### 任务清单

- [x] **3.1 新图标与登录 logo 设计（设计稿 + 源图）**

  - **改动内容**：产出品牌图标源图与登录页 logo 源图。
  - **设计方向（品牌语意）**：主题「余墨」（配色/调墨），主色 `<#16796d>` 青绿 + 深色 `<#062f2c>`；图形建议为「墨水滴落 + 色块/色环」——一个墨滴形轮廓内嵌三瓣 Lab 三通道色块（或一个渐变色滴），传达「余墨 + 颜色匹配」。避免 AI slop（无意义渐变光晕、多余圆角叠加）。
  - **规格**：
    - 应用图标源图：**1024×1024 PNG**（透明底，圆角矩形 20% 圆角，居中图形占 70%）。
    - 登录页 logo：**512×512 PNG**（透明底，与登录面板深色背景协调，用于替换 `login-logo.png`）。
  - **产出路径（草稿区）**：先放 `c:\Users\qwq12\.trae-cn\work\6a75291e683777247fef039e\` 供评审。
  - **新增依赖**：无（设计稿 + 图片生成）。
  - **验收标准**：设计稿评审通过；源图在 32px 与 256px 缩放下轮廓清晰可辨。

- [x] **3.2 生成 Tauri 图标集**

  - **改动内容**：由 1024 源图生成 `apps/desktop/src-tauri/icons/` 全套。
  - **命令**：`npx tauri icon <源图1024.png> --output apps/desktop/src-tauri/icons`（生成 `icon.ico / icon.icns / icon.png / 32x32.png / 128x128.png / 128x128@2x.png`）。
  - **同步仓库外源文件**：`prepare-desktop.mjs` 的 `iconSource` 指向仓库外 `../apps/desktop/src-tauri/icons/icon.ico`，构建时会把该文件复制回仓库内——必须**同步更新仓库外源 `icon.ico`**，否则 `desktop:build` 会用旧图标覆盖回仓库。
  - **新增依赖**：无（`@tauri-apps/cli` 已在 devDependencies）。
  - **验收标准**：`apps/desktop/src-tauri/icons/icon.ico` 与仓库外源文件均为新图标；`npx tauri icon` 输出无报错。

- [x] **3.3 `tauri.conf.json` 中文产品名**

  - **改动内容**：
    - `productName: "ResidualInkManagement"` → `"余墨管理系统"`。
    - `identifier` 保持 ASCII `com.residualink.management.desktop` 不变。
    - `app.windows[].title` 已是「余墨管理系统」，无需改。
  - **影响**：exe 名 → `余墨管理系统.exe`；NSIS 安装包名 → `余墨管理系统_1.0.0_x64-setup.exe`；安装目录、开始菜单、任务栏、卸载项均显示中文名。
  - **新增依赖**：无。
  - **验收标准**：`npx tauri info`/配置解析无错；`productName` 生效后安装包与安装名均为中文。

- [x] **3.4 `installer-hooks.nsh` 进程名更新**

  - **改动内容**：`NSIS_HOOK_PREINSTALL` 与 `NSIS_HOOK_PREUNINSTALL` 内的 `taskkill /F /IM ResidualInkManagement.exe /T` 改为 `taskkill /F /IM 余墨管理系统.exe /T`；**并保留旧行**作为升级路径兜底（旧版本残留进程仍可结束）。
  - **新增依赖**：无。
  - **验收标准**：安装/卸载时能结束中英文两个进程名；升级安装（从旧英文版覆盖）不因进程占用失败。

- [x] **3.5 登录页与侧栏品牌图更新**

  - **改动内容**：
    - 用 3.1 的登录 logo 替换 `apps/web/src/assets/login-logo.png`。
    - `LegacyApp` 侧栏品牌图标：`<DatabaseOutlined />` 换为 `<img src={loginLogo} className="rim-sider-logo" />`（复用同一登录 logo 或独立小图标），保持品牌一致。
  - **样式**：`styles.css` 新增 `.rim-sider-logo { width: 26px; height: 26px; object-fit: contain; }`。
  - **新增依赖**：无。
  - **验收标准**：登录页与侧栏均显示新 logo；深浅两种背景（登录深色面板、侧栏深色）下均清晰可见。

- [x] **3.6 打包与验收**

  - **改动内容**：`npm run desktop:build` 重新打包（先 `desktop:prepare` 同步图标/资源）。
  - **验收结果**：
    - 产出安装包 `安装包/余墨管理系统_1.0.0_x64-setup.exe`（65,270,161 字节）。
    - NSIS 安装包名为中文「余墨管理系统」；窗口标题、侧栏与登录页 logo 均为新品牌。
    - 注：打包环境需将 `C:\Users\qwq12\.cargo\bin` 加入 PATH，否则 `tauri build` 报 `cargo metadata: program not found`；`prepare-desktop.mjs` 已改用 `C:/Windows/System32/tar.exe` 绝对路径调用 tar。
  - **验收标准**：
    - 产出安装包 `安装包/余墨管理系统_1.0.0_x64-setup.exe`。
    - 安装后：开始菜单/任务栏/窗口标题显示「余墨管理系统」；任务栏图标为新图标；任务管理器进程名 `余墨管理系统.exe`。
    - 安装/卸载/升级（从旧英文版覆盖）均正常，无进程占用报错。

**阶段 3 验证**：执行 3.6 打包验收；核对安装包名、安装名、图标、进程名、卸载项；回归启动与登录。

---

## 四、假设与决策

- **账号记忆载体**：`window.localStorage`（Tauri WebView2 持久化到应用数据目录，可跨重启保留）。**仅存账号、不存密码**，密码框每次为空。
- **页面状态保持范围**：采用「懒挂载 + 常驻隐藏」keep-alive，仅覆盖**单次登录会话内**；退出登录即整体卸载清空（符合预期，不做跨会话持久化）。
- **keep-alive 不影响数据时效**：页面常驻后切回不再重新拉取（结果保留上次数据）；如需手动刷新，沿用各页既有「查询/刷新」按钮，不新增自动刷新。
- **中文产品名**：`productName` 中文化，`identifier` 保持 ASCII；NSIS 简体中文语言包已启用，中文安装名/安装包名在 Windows 下正常。
- **图标源**：图标源文件位于仓库外（`prepare-desktop.mjs` 的 `iconSource`），更换时必须仓库内、仓库外两处同步，否则 `desktop:build` 会回写旧图标。
- **实施顺序**：阶段 2（状态保持，业务影响最大）→ 阶段 1（账号记忆）→ 阶段 3（品牌与打包）。

---

## 五、验收与验证

1. 每阶段改动后运行 `npm run typecheck` 与 `npm test`（api + web vitest），确保零回归。
2. 阶段 1 新增 `session.test.ts`；阶段 2 无新增测试（手动走查）；阶段 3 以打包产物验收。
3. 手动 `npm run dev` 逐项验证：账号记忆（重启保留上次账号、下拉选择、清除、不记密码）；页面切换状态保持（2.3 全部勾选）；图表/表格/表单返回后保留。
4. 桌面端 `npm run desktop:build` 打包验收中文名、图标、安装/卸载/升级；登录页与侧栏 logo 实机核对。

---

_本文档为设计方案 + 实施清单。任务完成即在本文档勾选 `- [x]`，作为进度唯一记录。确认方案后再进入实施阶段。_