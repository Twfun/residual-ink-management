# 余墨管理系统 · 功能增强方案（含 Plan 进度勾选）

> 版本：v1.1　|　适用项目：`residual-ink-management`（React 19 + Ant Design 5 + NestJS + Prisma/MariaDB + Tauri 桌面端）
> 本文档为**设计方案 + 实施清单**，每条任务均带 `- [ ]` 勾选框。**完成即在本文档勾选**，作为进度的唯一记录。确认方案后再进入实施阶段。

---

## 〇、Plan 使用说明（勾选操作）

- 每个阶段下都有「阶段进度」总表与「任务清单」两个视图，二者指向同一组任务，任选其一勾选即可（建议以「任务清单」为准）。
- **勾选规则**：任务完成并通过该任务「验收标准」后，把 `- [ ]` 改为 `- [x]`；同时更新顶部「总体进度」的 `已完成/总数` 计数。
- **回滚规则**：若某任务上线后需回退，改回 `- [ ]` 并在该任务下追加一行 `> 回滚：<原因/时间>`，不删除记录。
- **纪律**：一个任务只勾一次；不允许「半完成」勾选，未过的验收标准一律保持 `- [ ]`。

### 总体进度

> 阶段 1：`5/5`　阶段 2：`5/5`　阶段 3：`8/8`　阶段 4：`5/5`　阶段 5：`3/3`　（已于 2026-08-06 全部实施并通过自动化验证）

---

## 一、背景与目标

「余墨管理系统」是产品色样管理系统的独立简易版，用于管理余墨库存、颜色匹配、出库、用户权限、备份恢复与操作日志。系统基础功能已可用，但经全面审查，存在**可视化缺失、已定义功能未落地、业务深度不足、UI 体验不统一**等"空窗"，需要增强。

用户已确认的目标：

| 方向               | 说明                                                  | 优先级   |
| ------------------ | ----------------------------------------------------- | -------- |
| ① 数据可视化与分析 | 让工作台从裸表格升级为可读的可视化面板                | 正常     |
| ② 补齐未落地功能   | 落地已定义但未实现的导出、deltaE、筛选、权限 Gate     | 正常     |
| ③ 业务功能深化     | 颜色匹配多公式、出库交互增强、匹配结果直接出库        | 高       |
| ④ 整体 UI/交互升级 | 引入 huashu-design 设计方向做增量打磨，不推翻现有框架 | 正常     |
| ⑤ 测量流程增强     | X-Rite 测量固定为真实仪器按压测量                     | **优先** |

**约束**：暂不新增独立业务模块；不新增权限点；新增运行时依赖为零；UI 沿用品牌主色 `#16796d` 与深色登录色 `#062f2c`。

---

## 二、当前状态分析

### 2.1 架构现状

- **前端** `apps/web`：单文件 `LegacyApp.tsx`（约 1000 行）承载全部 8 个页面（dashboard / match / inventory / outbound / users / backup / logs / config），配 `styles.css` 与 `contracts.ts`。
- **后端** `apps/api`：NestJS 11 + Prisma + MariaDB，按领域拆分 `dashboard.ts / inventory.ts / outbound.ts / match.ts / excel.ts / administration.ts / backup.ts / auth.ts`，统一 JWT + 权限守卫（`guards.ts`），权限/角色基线在 `PrismaService.ensureBaseline()`。
- **数据模型**（`prisma/schema.prisma`）：`ResidualInk`、`OutboundRecord`、`ColorMeasurement`、`ImportJob`、`OperationLog`、`UserAccount/Role/Permission/RolePermission`、`BackupJob`、`DesktopConfig`。
- **测试**：根 `npm test` = `test:api` + `test:web`（均为 vitest）。现有 `common.test.ts`（纯函数）、`contracts.test.ts`（契约），另有 `scripts/playwright-ui-smoke.mjs` 冒烟。
- **桌面端**：Tauri 2 封装 API exe + Web 前端，X-Rite 测色经 `xrite_command` 桥接（`exact-bridge.exe` + `eXact.dll`）。

### 2.2 关键发现（增强空间）

1. **可视化空转（高优先级）**：`echarts ^6.1.0` 已装在 web 依赖但从未使用；后端 `dashboard.ts` 已实现 `series` 端点（按日/周/月/年分桶的进出库趋势）与 `overview` 的 `period` 过滤，但前端 `Dashboard` 组件只消费 `statistics` 与两张最近记录表，**从未调用 `/dashboard/series`**。
2. **导出未落地**：`PERMISSIONS` 已定义 `inventory.export`、`outbound.export`、`logs.export`，但后端无导出端点；`excel.ts` 只做导入，无导出。
3. **deltaE 链路未接通**：`inventory.ts` 的 `list` 已支持 `targetL/targetA/targetB` 计算并排序 deltaE，但 `InventoryPage` 筛选表单未传这三个参数，deltaE 列恒为空。
4. **匹配深度不足**：`match.ts` 仅支持 CIE76 单公式；前端 `Match` 页未消费测量历史（`GET /match/measurements` 端点存在但未用），且 X-Rite 结果以 `<pre>` 输出原始 JSON。
5. **出库过滤未接通**：`outbound.ts` 的 `list` 已支持 `keyword/from/to`，但前端 `OutboundPage` 只拉全量，无日期范围筛选。
6. **权限细分未在页内落位**：除 `InventoryPage` 接收 `rights` 门控编辑外，其余页面按钮未按权限 Gate，仅靠菜单级隐藏。
7. **测量交互不完整（优先）**：`Match` 页与 `InventoryModal` 的测量均直接 `invoke('xrite_command', { command: 'measure 30000' })` 同步等待，成功后仅回填 L\*/a\*/b\*；无色块、无测量条件回填、无 60 秒超时与「重试测量」，也未区分「真实仪器按压读数」与「应用模拟触发」。
8. **UI 一致性**：8 个页面空态/加载态/卡片层级不统一，与整体体验脱节。

---

## 三、分阶段增强方案

> 路径均以 `d:\Projects\Product Color Sample Management System V4.0\residual-ink-management\` 为根。每阶段独立可交付、可回归、可回滚。实施顺序：阶段 1 → 2 → 3（优先）→ 4 → 5。

---

### 阶段 1：数据可视化与分析（方向①）

**目标**：把已具备的 `series` 数据与已安装的 echarts 用起来，补充多维聚合分析，让工作台成为可视化面板。

**阶段进度**：`5/5`

#### 任务清单

- [x] **1.1 新建 `apps/web/src/components/EChart.tsx`**

  - **改动内容**：轻量 echarts 封装，供全项目复用，避免引入 `echarts-for-react`。
  - **组件签名**：`EChart({ option, height }: { option: echarts.EChartsOption; height?: number | string })`，默认 `height = 300`。
  - **实现细节**：
    - `useRef<HTMLDivElement>` 挂载容器；`useEffect` 内 `echarts.init(el)` 初始化，`option` 变化时 `chart.setOption(option, { notMerge: true })`。
    - 用 `ResizeObserver` 监听容器尺寸变化，`chart.resize()`；`useEffect` 清理函数 `chart.dispose()`。
    - 导出 `EChart` 组件；`echarts` 树摇按需注册已在 `LegacyApp.tsx` 顶部（`BarChart/LineChart/GridComponent/TooltipComponent/LegendComponent/DataZoomComponent/CanvasRenderer`）。
  - **验收标准**：组件在 `Dashboard` 内渲染三张图无控制台报错；窗口缩放图自适应；组件卸载无泄漏告警。

- [x] **1.2 `apps/api/src/dashboard.ts` 新增 3 个聚合端点**

  - **改动内容**：`DashboardService` 新增三个方法，`DashboardController` 注册对应 `@Get` 路由，均加 `@RequirePermissions('dashboard.view')`。
  - **端点与契约**：
    - `GET /dashboard/color-distribution` → `{ rows: [{ colorFamily: string | null, count: number, weightKg: number | null }] }`。逻辑：`residualInk` 按 `status: '在库'` 过滤，`groupBy colorFamily`，同时统计 `count` 与 `_sum.weightKg`（`weightKg` 为 null 的不计入重量和）。
    - `GET /dashboard/location-rank?limit=10` → `{ rows: [{ storageLocation: string, weightKg: number }] }`。逻辑：`status: '在库'` 且 `weightKg != null`，按 `storageLocation` 分组求和，按重量降序取 `limit`（默认 10，`clamp 1..50`）。
    - `GET /dashboard/weight-distribution` → `{ buckets: [{ label: string, min: number | null, max: number | null, count: number }] }`。分档：`0-1 / 1-5 / 5-10 / 10-50 / 50+`（单位 kg），返回每档在库数量。
  - **新增依赖**：无。
  - **验收标准**：三端点返回结构与契约一致；空库返回空数组不报错；`limit` 越界被 clamp。

- [x] **1.3 `apps/web/src/LegacyApp.tsx` `Dashboard` 新增图表区块**

  - **改动内容**：`Dashboard` 组件在指标卡下方、最近记录表上方插入图表区。
  - **布局**：`.chart-grid`（CSS Grid，`minmax(260px, 1fr)` 自适应列）容纳三张卡片：
    1. **进出库趋势折线**：数据源 `GET /dashboard/series?dimension={dimension}`；卡片右上角 `Segmented` 切换 `日/周/月/年`（默认 `day`）；`option` 用 `LineChart`，两条序列 `inbound/outbound`，`xAxis` 用 `buckets[].label`，`tooltip` `trigger: 'axis'`。
    2. **色系分布饼图**：数据源 `GET /dashboard/color-distribution`；`PieChart`（需在 echarts 注册 `PieChart`），`colorFamily` 为空时的标签显示「未分类」。
    3. **库位重量 Top 条形图**：数据源 `GET /dashboard/location-rank?limit=10`；`BarChart` 横向条形图。
  - **echarts 注册补充**：`PieChart` 需在 `LegacyApp.tsx` 顶部的 `echarts/charts` 引入中追加。
  - **空态**：任一端点返回空数组时，卡片内显示「暂无数据」占位，不渲染空图。
  - **验收标准**：三张图随数据渲染；维度切换触发重新拉取；空数据有占位；窄屏（560px）图表收缩不溢出。

- [x] **1.4 `apps/web/src/styles.css` 图表样式**

  - **改动内容**：新增 `.chart-grid`（`display:grid; gap:16px; grid-template-columns:repeat(auto-fit,minmax(260px,1fr))`）、`.chart-card`（`.chart-card .ant-card-body{height:340px}`）、`.chart-empty`（居中灰字占位）。
  - **新增依赖**：无。
  - **验收标准**：900px/560px 断点下网格正确换行、图不溢出。

- [x] **1.5 `apps/web/src/contracts.ts` 新增常量**

  - **改动内容**：新增 `export const DASHBOARD_DIMENSIONS = [{ value: 'day', label: '日' }, { value: 'week', label: '周' }, { value: 'month', label: '月' }, { value: 'year', label: '年' }] as const;`
  - **新增依赖**：无。
  - **验收标准**：`contracts.test.ts` 断言 `DASHBOARD_DIMENSIONS` 长度与首项 `day`。

**阶段 1 验证**：新建 `apps/api/src/dashboard.test.ts` 对 `bucketKey / bucketLabel / nextBucket` 做边界单测（跨年/跨月/跨周）；`contracts.test.ts` 断言维度常量；手动验证三张图随数据渲染、无控制台报错、窄屏收缩正常。

---

### 阶段 2：补齐未落地功能（方向②）

**目标**：补齐导出端点，接通 deltaE、测量历史、出库筛选、页内权限 Gate。

**阶段进度**：`5/5`

#### 任务清单

- [x] **2.1 `apps/api/src/excel.ts` 新增三套导出端点**

  - **改动内容**：在 `ExcelController`（或新建 `ExportController`）注册三个 `@Get` 导出端点，用 exceljs 服务端生成 `.xlsx` 并以 `Stream` 流式返回。
  - **端点与权限**：
    - `GET /excel/export/inventory?keyword=&status=`，`@RequirePermissions('inventory.export')`。
    - `GET /excel/export/outbound?keyword=&from=&to=`，`@RequirePermissions('outbound.export')`。
    - `GET /excel/export/logs?keyword=&from=&to=&operationType=`，`@RequirePermissions('logs.export')`。
  - **实现细节**：
    - 复用既有查询条件（`inventory.list` / `outbound.list` 的 where 构造；`logs` 查询逻辑抽到 2.2 复用）。
    - 表头用中文（库位/版辊号+色序/入库日期/重量/L/a/b/色差/色系/备注2/备注3；出库单用出库单号/出库日期…；日志用操作人/操作类型/目标表/目标ID/备注/时间）。
    - 响应头：`Content-Type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`，`Content-Disposition: attachment; filename*=UTF-8''inventory_YYYYMMDD_HHmmss.xlsx`。
    - 用 `body` 为 `Stream`，NestJS 通过 `@Res({ passthrough: false })` 或返回 `StreamableFile`。
  - **新增依赖**：无（exceljs 已在 api 依赖）。
  - **验收标准**：三套导出文件可用 Excel 打开、表头中文正确、参数映射无误；无权限返回 403。

- [x] **2.2 `apps/api/src/administration.ts` 抽取日志查询复用方法**

  - **改动内容**：把 `logs` 查询逻辑抽成 `buildLogWhere(query)` 与 `queryLogs(query)` 两个私有/公开方法，供 `GET /logs` 与日志导出复用。
  - **验收标准**：`GET /logs` 行为不变；导出复用同一过滤。

- [x] **2.3 `apps/api/src/app.module.ts` 注册导出控制器**

  - **改动内容**：确认导出方法/控制器已注册进 `AppModule` 的 `controllers` 与 `providers`。
  - **验收标准**：`npm run dev` 后导出路由可访问、Swagger（如有）可见。

- [x] **2.4 `apps/web/src/LegacyApp.tsx` 前端接线**

  - **改动内容**（5 处）：
    1. **deltaE 生效**：`InventoryPage` 筛选表单新增 `targetL/targetA/targetB` 三个 `InputNumber`，`onFinish` 时并入 `filters` 透传给 `/inventory?targetL=&targetA=&targetB=`；后端已支持，前端补传后 deltaE 列即出现并按升序。
    2. **导出按钮**：`InventoryPage` / `OutboundPage` / `LogsPage` 工具栏加「导出 Excel」按钮。实现：`fetch(\`${API}/excel/export/xxx?...\`, { headers: { Authorization: Bearer } })`→`res.blob()`→`URL.createObjectURL`+ 临时`<a download>`触发下载；按钮`loading`态标识「导出中」。按`inventory.export / outbound.export / logs.export` Gate，无权限隐藏。
    3. **测量历史**：`Match` 页新增「测量历史」区块，`useEffect` 调 `GET /match/measurements?limit=30`，用 `SimpleTable` 展示 `measuredAt / lStar / aStar / bStar / densityT / instrumentModel / measureCondition / measuredBy`。
    4. **出库日期筛选**：`OutboundPage` 工具栏加 `DatePicker.RangePicker`，值变化后把 `from/to`（`YYYY-MM-DD`）透传 `/outbound?from=&to=`
    5. **页内权限 Gate**：`Match`/`Outbound`/`Logs`/`Users`/`Backup`/`Config` 页接收 `rights` prop，页内按钮按权限显示（如 `BackupPage` 的「执行备份」按 `backup.manage`、`LogsPage` 导出按 `logs.export`）。
  - **新增依赖**：无（前端导出用原生 `fetch` + Blob）。
  - **验收标准**：
    - 输入目标 Lab → deltaE 列出现并按升序；清空后 deltaE 列恢复为空。
    - 三页导出可下载且 Excel 可打开；无权限账号看不到导出按钮。
    - 出库日期筛选生效；测量历史列表出现最近记录。

- [x] **2.5 `apps/web/src/contracts.ts` / `styles.css` 配套**

  - **改动内容**：`contracts.ts` 新增导出/筛选相关常量（如导出按钮文案、出库日期默认）；`styles.css` 新增 `.toolbar-actions`（筛选条右侧操作区）、导出按钮下载中样式。
  - **验收标准**：`contracts.test.ts` 断言新常量；页面工具栏布局正常。

**阶段 2 验证**：新建 `apps/api/src/excel.test.ts` 对三套导出表头与参数映射单测；手动导出库存/出库/日志可用 Excel 打开；输入目标 Lab 后 deltaE 列出现并按升序；出库日期筛选生效；测量历史列表出现；无权限账号看不到对应按钮。

---

### 阶段 3：测量流程增强（优先 · 真实仪器按压测量）

**目标**：把 X-Rite 测量规范化为「点击测量 → 等待真实按压读数 → 成功自动回填并关闭 / 失败重试」的完整交互，贯穿 `Match` 页与 `InventoryModal`。测量读取支持**两种触发方式**，成功后均自动回填读数并关闭弹窗：

- **自动触发（默认）**：打开测量弹窗后即自动进入等待状态，用户将 eXact 按向基座，收到完成读数后自动回填并关闭。
- **手动触发**：用户也可在弹窗内点击「开始测量」按钮主动发起测量，收到完成读数后同样自动回填读数并关闭窗口。

**统一测量弹窗组件**（新建 `apps/web/src/components/MeasureModal.tsx`）：供 `Match` 页与 `InventoryModal` 复用，避免两份测量逻辑漂移。

**阶段进度**：`8/8`

#### 任务清单

- [x] **3.1 新建 `apps/web/src/components/MeasureModal.tsx`（弹窗骨架与触发）**

  - **组件契约**：`MeasureModal({ open, onCancel, onSuccess }: { open: boolean; onCancel: () => void; onSuccess: (measurement: MeasuredResult) => void })`。
  - **触发入口**：`Match` 页点「X-Rite 测量」、`InventoryModal` 点「仪器测量 Lab」时，`open` 置 `true` 弹出弹窗（见 3.5 / 3.6）；弹窗标题为**「测量当前油墨调合色」**。
  - **弹窗 UI 结构（自上而下，参考设计稿）**：
    1. **标题栏**：左对齐标题「测量当前油墨调合色」+ 右上角关闭「×」。
    2. **警告提示区**（仅错误态显示）：浅红底 `#FFF2F0` + 红边框，红色圆形「×」图标 + 加粗标题「本次测量未完成」+ 详情文案「30 秒内未获取到仪器测量结果，请重新测量。请重新将 eXact 按向目标基座后点击『重新开始监听』」。
    3. **测量信息区**：左侧「测量色块」预览框（等待态浅灰 `#F5F5F5`，下注「等待测量」；成功态渲染 Lab 色块）；右侧状态参数表格 2 列——「连接状态 / 已连接」「测量条件 / M0」。
    4. **偏好复选框**：「自动完成并关闭窗口」（勾选后成功读数自动 `onSuccess` 并关闭，默认勾选）。
    5. **操作按钮区**：右下「取消」（次要）+「开始测量 / 重新开始监听」（主要，按状态切换文案）。
  - **状态机**：`status: 'idle' | 'waiting' | 'measuring' | 'success' | 'error'`，`measurement: MeasuredResult | null`，`error: string | null`。
  - **行为**：
    - `open` 变 `true` 时自动把 `status` 置为 `waiting`（自动触发，等待真实按压）。
    - `waiting`/`measuring` 态显示引导文案「将 eXact 用力按向目标基座，保持到仪器显示『完成！』与 Lab 数据」。
    - 提供「开始测量」按钮（手动触发）：点击后 `status → measuring` 并发起测量。
    - 收到读数后 `status → success`，展示色块与 Lab；若「自动完成并关闭窗口」勾选，短暂（约 600ms）后自动 `onSuccess(measurement)` 并关闭弹窗。
    - 超时进入 `error`：透出红色警告卡片 + 按钮文案切换为「重新开始监听」。
  - **新增依赖**：无。
  - **验收标准**：点测量按钮弹出该弹窗；打开即出现引导语；点「开始测量」可手动发起；成功（勾选自动关闭）自动回调并关闭；错误态展示红色警告卡片且按钮变为「重新开始监听」。

- [x] **3.2 同上（Lab 色块显示）**

  - **改动内容**：新增 `labToRgb` 纯函数（Lab→sRGB 标准转换，含色域裁剪），读数成功后渲染色块。
  - **`labToRgb` 实现要点**：`L/100` → 归一化；`Lab→XYZ`（D65, 2° 标准观察者）→ `XYZ→sRGB`（线性到 gamma 编码）→ 裁剪到 `[0,255]` 并四舍五入。入参 `{ l, a, b }` 出参 `{ r, g, b }`。
  - **UI**：`.color-swatch` 撑起色块，下方展示 `L*/a*/b*` 与测量条件。
  - **新增依赖**：无（Lab→RGB 为自实现纯函数）。
  - **验收标准**：`labToRgb` 已知样例断言——白 `L100 a0 b0 → rgb(255,255,255)`；中灰 `L50 a0 b0 → ≈rgb(119,119,119)`；纯红 `L53.24 a80.09 a67.2 → ≈rgb(255,0,0)`（误差 ±2）。

- [x] **3.3 同上（超时与重试 /「重新开始监听」）**

  - **改动内容**：测量 Promise 与超时定时器（默认 60s，参考稿为 30s，做成常量 `MEASURE_TIMEOUT_MS` 便于调整）`Promise.race`；超时将 `status → error`，显示红色警告卡片（标题「本次测量未完成」+ 详情文案）与「重新开始监听」主按钮、「取消」按钮。
  - **行为**：「重新开始监听」与「开始测量」走完全相同的发起 + 回填 + 自动关闭逻辑；**不提供手工录入入口**。
  - **验收标准**：超时未按压显示红色警告态与「重新开始监听」按钮；重试后读数回填与关闭逻辑一致；无法通过手工输入绕过测量。

- [x] **3.4 同上（测量条件回填）**

  - **改动内容**：从测量响应解析 `measureCondition / instrumentModel / instrumentSerial / densityT`，封装进 `MeasuredResult` 一并随 `onSuccess` 返回。
  - **类型**：`type MeasuredResult = { l: number; a: number; b: number; measureCondition?: string | null; instrumentModel?: string | null; instrumentSerial?: string | null; densityT?: number | null; raw?: unknown }`。
  - **验收标准**：`onSuccess` 携带完整测量条件；字段缺失时安全回退 `null`。

- [x] **3.5 `apps/web/src/LegacyApp.tsx`（`Match` 页接入）**

  - **改动内容**：用 `MeasureModal` 替换 `measureWithXrite`。
  - **行为**：点「X-Rite 测量」打开 `MeasureModal`；`onSuccess(m)` 回调里 `form.setFieldsValue({ l: m.l, a: m.a, b: m.b, measureCondition: m.measureCondition })`，并把 `m.raw` 保留用于 `POST /match/measurements`（body：`{ l, a, b, source: 'instrument', densityT, serial, model, measureCondition }`）；移除 `<pre>` 输出。
  - **验收标准**：测量后自动回填，无原始 JSON；可再次搜索。

- [x] **3.6 `apps/web/src/LegacyApp.tsx`（`InventoryModal` 接入）**

  - **改动内容**：用 `MeasureModal` 替换 `measureLab`。
  - **行为**：点「仪器测量 Lab」打开弹窗；`onSuccess(m)` 回调 `form.setFieldsValue({ lStar: m.l, aStar: m.a, bStar: m.b, measureCondition: m.measureCondition })` 并自动关闭弹窗。
  - **验收标准**：测量后自动回填与关闭；保存时 `measureCondition` 一并落库。

- [x] **3.7 `apps/api/src/common.ts` 新增 `labToRgb`（或 web 侧实现）**

  - **改动内容**：选择后端 `common.ts` 实现（便于单测）或仅 web 侧实现并单测；若后端实现则导出供测试。
  - **新增依赖**：无。
  - **验收标准**：`common.test.ts`（或 `labToRgb.test.ts`）含白/中灰/纯红样例断言。

- [x] **3.8 `apps/web/src/styles.css` 测量相关样式**

  - **改动内容**：`.color-swatch`（Lab 色块，`width:100%; height:72px; border-radius:6px; border:1px solid #e2e8e7`）、`.measure-guide`（引导文案配色）、`.measure-error`（红色警告卡片：浅红底 `#FFF2F0` + 红边框 + 红色图标 + 加粗错误标题）、`.measure-status-table`（连接状态/测量条件 2 列表格）、`.measure-options`（「自动完成并关闭窗口」复选框行）。
  - **验收标准**：各状态（等待/成功/错误）样式在桌面端显示正常，与参考设计稿一致。

**阶段 3 验证**：自动化 `labToRgb` 样例断言；桌面版 `Match` 页与库存弹窗点「测量」时弹出「测量当前油墨调合色」弹窗：① 自动触发——弹窗打开即出现引导语，按压后自动显示色块与 L\*/a\*/b\*/测量条件，勾选「自动完成并关闭窗口」则自动关闭/回填（默认勾选）；② 手动触发——在弹窗内点「开始测量」后按压仪器，同样自动回填读数并关闭窗口；③ 超时未按压显示红色警告卡片「本次测量未完成」与「重新开始监听」按钮，重试后读数回填与关闭逻辑一致；全程无 `<pre>` 原始 JSON、无手工录入入口。

---

### 阶段 4：业务功能深化（方向③）

**目标**：颜色匹配支持多公式与精细化筛选；出库交互增强；**颜色匹配结果可在列表内直接出库**。

**阶段进度**：`5/5`

#### 任务清单

- [x] **4.1 `apps/api/src/common.ts` 新增 `cie94`、`ciede2000`**

  - **改动内容**：新增两个色差纯函数，保持 `cie76` 不动。
  - **`cie94(left, right, opts?)` 实现要点**：默认制程色 `kl=1, k1=0.045, k2=0.015`；`ΔL*=L1-L2`，`C1=√(a1²+b1²)`，`C2=√(a2²+b2²)`，`ΔC=C1-C2`，`ΔH²=ΔE76²-ΔL²-ΔC²`（`ΔH=√max(0, ΔH²)`）；`ΔE94=√((ΔL/kl)²+(ΔC/(1+k1·C1))²+(ΔH/(1+k2·C1))²)`。
  - **`ciede2000(left, right)` 实现要点**：标准 CIEDE2000——`C̄=(C1+C2)/2`，`G=0.5·(1-√(C̄⁷/(C̄⁷+25⁷)))`，修正 `a1'=(1+G)a1, a2'=(1+G)a2`，重算 `C1',C2',h1',h2'`；`Δh'` 处理象限与 360° 环绕；`ΔL'=L1-L2,ΔC'=C1'-C2',ΔH'=2√(C1'C2')·sin(Δh'/2)`；`T=1-0.17cos(h̄'-30°)+0.24cos(2h̄')+0.32cos(3h̄'+6°)-0.20cos(4h̄'-63°)`；`Δθ=30exp(-((h̄'-275)/25)²)`；`Rc=2√(C̄'⁷/(C̄'⁷+25⁷))`；`Rt=-sin(2Δθ)·Rc`；`SL=1+0.015(L̄'-50)²/√(20+(L̄'-50)²)`，`SC=1+0.045C̄'`，`SH=1+0.015C̄'T`；`ΔE00=√((ΔL'/SL)²+(ΔC'/SC)²+(ΔH'/SH)²+Rt·(ΔC'/SC)·(ΔH'/SH))`。
  - **新增依赖**：无（全自实现）。
  - **验收标准**：`common.test.ts` 用标准已知色对断言（如 Sharma 2005 CIEDE2000 测试色对，误差 ≤1e-4）；`cie76` 回归通过。

- [x] **4.2 `apps/api/src/match.ts` `search` 增加筛选参数**

  - **改动内容**：`MatchInput` 增加 `formula? / maxDeltaE? / limit? / colorFamily?`；`search` 返回 `formula` 与 `matches`。
  - **实现细节**：
    - `formula`：`'CIE76' | 'CIE94' | 'CIEDE2000'`，默认 `'CIE76'`；按所选公式计算 `deltaE`（`cie76/cie94/ciede2000`）。
    - `maxDeltaE`：`numberOrNull`，非空则过滤 `deltaE <= maxDeltaE`。
    - `limit`：默认不截断；为正则 `slice(0, limit)`（连接 `availableCount` 为过滤后总数，`matches.length` 为返回条数）。
    - `colorFamily`：`text`，非空则匹配 `colorFamily === colorFamily`（精确匹配）。
    - 排序：`deltaE` 升序，同名取 `storageLocation` 升序（保持既有）。
  - **新增依赖**：无。
  - **验收标准**：新建 `match.test.ts` 校验——公式切换改变排序；阈值过滤生效；`limit` 截断；色系过滤生效；`availableCount` 语义正确。

- [x] **4.3 `apps/web/src/LegacyApp.tsx` `Match` 页筛选表单增强**

  - **改动内容**：`Match` 页「目标颜色」表单追加一行筛选控件。
  - **控件**：公式下拉（`Select`：CIE76/CIE94/CIEDE2000，默认 CIE76）、色差阈值 `InputNumber`（可选，`min>0`）、top-N `InputNumber`（可选，`min>=1`）、色系 `Select allowClear`（选项来自 `colorFamily` 常量，见 5.3）。
  - **展示**：结果表标题显示当前公式（如「颜色匹配结果（CIE94 · 12 条）」）；结果区顶部可加 `Tag` 展示公式与阈值。
  - **新增依赖**：无。
  - **验收标准**：切换公式/阈值/top-N/色系后重新搜索，结果与后端一致；控件缺省时行为与现在一致（默认 CIE76 全量）。

- [x] **4.4 `apps/web/src/LegacyApp.tsx`（出库单创建增强）**

  - **改动内容**：`OutboundPage`「创建出库单」卡片增强。
  - **实现细节**：
    - 新增 `DatePicker`（默认今天），值随 `POST /outbound` 的 `outboundDate`（`YYYY-MM-DD`）提交。
    - 明细行（`.outbound-line`）在库位 `Tag` 旁显示该库位当前剩余重量提示（如「当前 12.5 kg」），数据来自 `inventory.find(id)` 的 `weightKg`。
    - 出库重量 `InputNumber` 的 `max` 设为当前库存重量，`min=0.001`，`precision=3`。
  - **新增依赖**：无。
  - **验收标准**：出库日期默认今天且可改；明细行显示剩余量；超库存时前端即时拦截 + 后端兜底报错。

- [x] **4.5 `apps/web/src/LegacyApp.tsx`（匹配结果直接出库）**

  - **改动内容**：`Match` 结果表内直接出库。
  - **实现细节**：
    - `Match` 组件接收 `rights` prop（由 `LegacyApp` 传入 `user.permissions`）；`SimpleTable` 增加可选 `action` 列渲染函数。
    - 结果表新增「出库」操作按钮，仅 `rights.includes('outbound.create')` 时显示，否则整列隐藏。
    - 点击「出库」打开 `QuickOutboundModal`（`antd Modal` + `Descriptions` + `Form`）：
      - 预填并展示该余墨：`storageLocation`、`rollerColorCode`、`colorFamily`、**当前库存重量** `weightKg`。
      - 表单字段：本次出库重量 `InputNumber`（`min=0.001`、`precision=3`、`max=当前库存`，`required`）。
      - 提交：`POST /outbound`，body `{ outboundNo: '', lines: [{ residualInkId: record.id, weightKg }] }`（`outboundNo` 留空由后端 `nextNumber()` 自动生成）。
      - 前端预校验：重量必填、>0、≤当前库存；超限 `message.warning` 拦截。
  - **成功后刷新**：`message.success('出库成功。')` 关闭弹窗，并重新执行当前 `Match` 搜索（用表单当前 `l/a/b` 与筛选参数重跑 `search`），使该库位重量/状态变化反映到结果。
  - **新增依赖**：无（复用既有 `POST /outbound`）。
  - **验收标准**：有 `outbound.create` 权限账号点「出库」→ 填重量 → 提交后该库位重量/状态更新、结果刷新、出库记录与操作日志生成；无权限账号看不到「出库」按钮；出库重量超过库存被拦截；库位重量未知时提示「库位重量未知，请确认后手动填写」且允许提交（后端校验兜底）。

**阶段 4 验证**：`common.test.ts` 增 `cie94 / ciede2000` 标准色对断言并保持 `cie76` 回归；新建 `match.test.ts` 校验公式排序、阈值过滤、limit 截断、色系过滤；手动：`Match` 页结果表点「出库」→ 填本次重量 → 提交后该库位重量/状态更新、结果刷新、出库记录与日志生成；无 `outbound.create` 权限账号看不到「出库」按钮；出库日期与剩余量提示正确。

---

### 阶段 5：整体 UI/交互升级（方向④，huashu-design 增量应用）

**原则**：不推翻现有框架，延续品牌主色 `#16796d` 与深色登录色 `#062f2c`，做「一个细节做到 120%、其余 80%」的克制打磨；数据密集型产品走高密度信息层级；避免装饰性 icon 堆砌、避免无品牌语意的渐变/圆角+左色条等 AI slop。

**阶段进度**：`3/3`

#### 任务清单

- [x] **5.1 `apps/web/src/styles.css` 统一设计 token**

  - **改动内容**：统一卡片间距网格（`16px` 基准）、阴影/圆角级别、标题层级、表格密度；空态与加载态占位；图表配色对齐品牌（`#16796d` + 中性灰阶）；完善 responsive 规则（900px/560px 断点）。
  - **验收标准**：8 页间距/层级/空态/加载态一致；900px/560px 收缩正常。

- [x] **5.2 `apps/web/src/LegacyApp.tsx` 抽取公共组件**

  - **改动内容**：
    - 抽 `PageHeader`、`MetricCard`、`EmptyState`、`SectionCard` 公共组件，替换各页重复结构。
    - `Match` 页 X-Rite 原始 JSON（`<pre>`）改为结构化 `Descriptions`/标签展示。
    - 统一各页工具栏与空态文案；登录/顶栏/侧栏细节质感微调。
  - **验收标准**：页面结构统一、无重复 JSX；控制台无报错。

- [x] **5.3 `apps/web/src/contracts.ts` 新增 UI 常量**

  - **改动内容**：新增色系选项常量（`COLOR_FAMILIES`）、图表维度标签（可由 `DASHBOARD_DIMENSIONS` 复用）、公式选项（`MATCH_FORMULAS`）。
  - **验收标准**：`contracts.test.ts` 断言新增 UI 常量。

**阶段 5 验证**：`contracts.test.ts` 断言新增 UI 常量；手动逐页走查 8 个页面，确认间距/层级/空态/加载态一致、900px/560px 断点收缩正常、控制台无报错；可选 `scripts/playwright-ui-smoke.mjs` 冒烟回归。

---

## 四、假设与决策

- **范围**：四方向全做 + 测量流程优先；暂不新增业务模块、不新增权限点。
- **依赖**：新增运行时依赖为零。`echarts ^6.1.0` 与 `exceljs ^4.4.0` 已在依赖中直接启用；CIE94/CIEDE2000 与 Lab→RGB 自实现纯函数；前端导出用原生 `fetch` + Blob。
- **deltaE 默认**：保持 CIE76 为默认，避免破坏既有行为，新增公式为可选。
- **series 数据已具备**：后端 `dashboard.ts` 的 `series` 端点已存在，本方案仅需前端消费，不重复实现后端趋势逻辑。
- **测量流程**：等待读数统一走 `measure` 命令（真实仪器按压，不再应用模拟触发）；测量弹窗不提供手工录入入口，避免误改实测值；测量条件随读数回填并随 `ColorMeasurement` 保存。
- **匹配结果出库**：复用既有 `POST /outbound` 单条出库，`outboundNo` 留空自动生成；合适地复用 `SimpleTable` 的 `action` 列，避免新增整页。
- **实施顺序**：阶段 1 → 2 → 3（测量优先）→ 4 → 5；阶段 2 的 deltaE 接线与权限 Gate、阶段 3 的测量弹窗是阶段 4/5 的前置基础。

---

## 五、验收与验证

1. 每阶段改动后运行 `npm run typecheck`（双端 `tsc --noEmit`）与 `npm test`（api + web vitest），确保零回归。
2. 按阶段新增测试：阶段 1 `dashboard.test.ts`；阶段 2 `excel.test.ts`；阶段 3 `labToRgb.test.ts`（或扩展 `common.test.ts`）；阶段 4 扩展 `common.test.ts` + 新建 `match.test.ts`；阶段 5 扩展 `contracts.test.ts`。
3. 手动 `npm run dev` 逐页验证（工作台图表、导出、deltaE、测量弹窗、匹配多公式与直接出库、出库日期筛选、UI 一致性）。
4. 桌面端测量用 `npm run desktop:dev` 实机验证（按压读数、色块、回填、60s 超时重试）；桥接资源用 `npm run xrite:verify`；整体冒烟可用 `npm run standalone:smoke` 与 `scripts/playwright-ui-smoke.mjs`。

---

_本文档为设计方案 + 实施清单。任务完成即在本文档勾选 `- [x]`，作为进度唯一记录。确认方案后再进入实施阶段。_
